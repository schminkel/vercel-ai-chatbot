# Generate a random secret: https://generate-secret.vercel.app/32 or `openssl rand -base64 32`
AUTH_SECRET=****

# The following keys below are automatically created and
# added to your environment when you deploy on vercel

# Get your xAI API Key here for chat and image models: https://console.x.ai/
XAI_API_KEY=****

# Get your Anthropic API Key here for Claude models: https://console.anthropic.com/
ANTHROPIC_API_KEY=****

# AWS S3 Configuration for file storage
AWS_ACCESS_KEY_ID=****
AWS_SECRET_ACCESS_KEY=****
AWS_REGION=us-east-1
AWS_S3_BUCKET_NAME=your-chatbot-files-bucket

# Public environment variables for frontend (these will be exposed to the browser)
NEXT_PUBLIC_AWS_REGION=us-east-1
NEXT_PUBLIC_AWS_S3_BUCKET_NAME=your-chatbot-files-bucket

# Instructions to create a PostgreSQL database here: https://vercel.com/docs/storage/vercel-postgres/quickstart
POSTGRES_URL=****

# Setup POSTGRESQL
# docker run --name vercel-ai-chatbot-postgres -e POSTGRES_USER=postgres -e POSTGRES_PASSWORD=postgres -e POSTGRES_DB=vercel_ai_chatbot -p 5432:5432 -d postgres:16

# Setup Redis
# docker run --name vercel-ai-chatbot-redis -p 6379:6379 -d redis:7-alpine

# Instructions to create a Redis store here:
# https://vercel.com/docs/redis
REDIS_URL=****

NEXT_PUBLIC_HTTPS_USAGE=false